'use client'
export const ReadMoreComments = ({id}:{id: string}) => {
    return (
        <span className="cursor-pointer text-sm text-[#3C6E71] hover:underline" onClick={()=>{}}>
            Read more comments
        </span>
    )
}